using Domain.Entities;
using System;
using System.Collections.Generic;
using System.Text;

namespace Domain.Interfaces
{
    public interface IRoomTypeRepository
    {
        //Task<RoomType> AddAsync(RoomType roomType,CancellationToken cancellationToken);
        //Task<RoomType> GetAsync(Guid id, CancellationToken cancellationToken);
        //Task<IEnumerable<RoomType>> GetAllAsync(CancellationToken cancellationToken);
        //Task<RoomType> UpdateAsync(RoomType roomType,CancellationToken cancellationToken);
        //Task<bool> DeleteAsync(Guid id, CancellationToken cancellationToken);
    }
}
